import React from "react";
import { DashboardTemplate } from "./Template/DashboardTemplate";

interface AddProps {
   text: string;
}

export const Add = (props: AddProps): any => {
   return <DashboardTemplate>Add</DashboardTemplate>;
};
